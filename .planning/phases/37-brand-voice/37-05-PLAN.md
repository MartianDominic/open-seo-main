---
phase: 37-brand-voice
plan: 05
type: execute
wave: 4
depends_on: [37-02, 37-03, 37-04]
files_modified:
  - src/routes/_app/clients/$clientId/voice/index.tsx
  - src/routes/_app/clients/$clientId/voice/route.tsx
  - src/client/components/voice/VoiceSettingsTabs.tsx
  - src/client/components/voice/VoiceProfileSummary.tsx
  - src/client/components/voice/ToneTab.tsx
  - src/client/components/voice/VocabularyTab.tsx
  - src/client/components/voice/WritingTab.tsx
  - src/client/components/voice/ProtectionTab.tsx
  - src/client/components/voice/VoiceWizard.tsx
  - src/client/components/voice/VoicePreviewSuite.tsx
  - src/client/components/voice/ProtectionRulesEditor.tsx
autonomous: false
requirements: []

must_haves:
  truths:
    - "Voice settings accessible at /clients/[clientId]/settings/voice"
    - "Tabbed interface with Tone, Vocabulary, Writing, SEO Rules, Protection tabs"
    - "Always-visible voice profile summary sidebar"
    - "Full preview suite generates headline, paragraph, CTA samples"
    - "Visual protection rules editor with test functionality"
    - "Guided wizard for mode selection with decision tree"
    - "Real-time progress during voice learning"
  artifacts:
    - path: "src/routes/_app/clients/$clientId/voice/index.tsx"
      provides: "Main voice settings page route"
      min_lines: 100
    - path: "src/client/components/voice/VoiceSettingsTabs.tsx"
      provides: "Tabbed interface container"
      exports: ["VoiceSettingsTabs"]
    - path: "src/client/components/voice/VoiceWizard.tsx"
      provides: "Guided mode selection wizard"
      exports: ["VoiceWizard"]
    - path: "src/client/components/voice/VoicePreviewSuite.tsx"
      provides: "Sample generation with compliance scores"
      exports: ["VoicePreviewSuite"]
    - path: "src/client/components/voice/ProtectionRulesEditor.tsx"
      provides: "Visual rule editor with CSV import"
      exports: ["ProtectionRulesEditor"]
  key_links:
    - from: "src/routes/_app/clients/$clientId/voice/index.tsx"
      to: "src/serverFunctions/voice.ts"
      via: "useQuery/useMutation hooks"
      pattern: "getVoiceProfileFn|updateVoiceProfileFn"
    - from: "src/client/components/voice/VoicePreviewSuite.tsx"
      to: "src/serverFunctions/voice.ts"
      via: "generatePreviewFn call"
      pattern: "generatePreviewFn"
---

<objective>
Build the agency-grade voice settings UI.

Purpose: Provide an intuitive interface for agencies to configure voice profiles, preview generated content, manage protection rules, and learn voice from existing content. The UI must be comprehensive yet approachable.

Output: Full voice settings page with tabbed interface, preview suite, protection rules editor, and guided wizard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/37-brand-voice/37-CONTEXT.md
@.planning/phases/37-brand-voice/37-01-SUMMARY.md
@.planning/phases/37-brand-voice/37-03-SUMMARY.md
@.planning/phases/37-brand-voice/37-04-SUMMARY.md

<interfaces>
<!-- Existing UI patterns to follow -->

From src/routes/_app/clients/$clientId/connections/index.tsx:
```typescript
import { createFileRoute, Link, useParams } from "@tanstack/react-router";
import { useQuery, useMutation, useQueryClient } from "@tanstack/react-query";
import { Button } from "@/client/components/ui/button";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/client/components/ui/card";
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/client/components/ui/table";

export const Route = createFileRoute("/_app/clients/$clientId/connections/")({
  component: ConnectionsListPage,
});

function ConnectionsListPage() {
  const { clientId } = useParams({ from: "/_app/clients/$clientId/connections/" });
  const { data, isLoading } = useQuery({
    queryKey: ["connections", clientId],
    queryFn: () => getConnectionsFn({ data: { clientId } }),
  });
  // ...
}
```

From @tevero/ui components (available):
- Card, Tabs, TabsList, TabsTrigger, TabsContent
- RadioGroup, RadioGroupItem
- Slider
- Badge
- Dialog, DialogContent, DialogHeader, DialogTitle
- Select, SelectContent, SelectItem, SelectTrigger, SelectValue
- Input, Textarea
- Button
- Progress (for voice learning progress)
- Alert, AlertDescription

From src/serverFunctions/voice.ts (from 37-03):
```typescript
export const getVoiceProfileFn = createServerFn({ method: "GET" });
export const updateVoiceProfileFn = createServerFn({ method: "POST" });
export const getProtectionRulesFn = createServerFn({ method: "GET" });
export const createProtectionRuleFn = createServerFn({ method: "POST" });
export const importProtectionRulesCsvFn = createServerFn({ method: "POST" });
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create route and main page structure</name>
  <files>src/routes/_app/clients/$clientId/voice/index.tsx, src/routes/_app/clients/$clientId/voice/route.tsx</files>
  <action>
Create route.tsx for the route layout:

```typescript
import { createFileRoute, Outlet } from "@tanstack/react-router";

// @ts-expect-error - Route not yet in generated route tree
export const Route = createFileRoute("/_app/clients/$clientId/voice")({
  component: VoiceLayout,
});

function VoiceLayout() {
  return <Outlet />;
}
```

Create index.tsx as the main voice settings page:

```typescript
import { createFileRoute, useParams } from "@tanstack/react-router";
import { useQuery } from "@tanstack/react-query";
import { Loader2 } from "lucide-react";
import { getVoiceProfileFn, getClientFn } from "@/serverFunctions/voice";
import { VoiceSettingsTabs } from "@/client/components/voice/VoiceSettingsTabs";
import { VoiceProfileSummary } from "@/client/components/voice/VoiceProfileSummary";
import { VoiceWizard } from "@/client/components/voice/VoiceWizard";

// @ts-expect-error - Route not yet in generated route tree
export const Route = createFileRoute("/_app/clients/$clientId/voice/")({
  component: VoiceSettingsPage,
});

function VoiceSettingsPage() {
  const { clientId } = useParams({ from: "/_app/clients/$clientId/voice/" });
  
  const { data: profile, isLoading } = useQuery({
    queryKey: ["voice-profile", clientId],
    queryFn: () => getVoiceProfileFn({ data: { clientId } }),
  });
  
  if (isLoading) {
    return (
      <div className="flex items-center justify-center h-64">
        <Loader2 className="w-6 h-6 animate-spin" />
      </div>
    );
  }
  
  // No profile yet - show wizard
  if (!profile) {
    return (
      <div className="container mx-auto py-8 max-w-4xl">
        <VoiceWizard clientId={clientId} />
      </div>
    );
  }
  
  // Existing profile - show tabs with sidebar summary
  return (
    <div className="container mx-auto py-8">
      <div className="grid grid-cols-1 lg:grid-cols-4 gap-6">
        {/* Sidebar summary - always visible */}
        <div className="lg:col-span-1">
          <VoiceProfileSummary profile={profile} />
        </div>
        
        {/* Main tabbed interface */}
        <div className="lg:col-span-3">
          <VoiceSettingsTabs profile={profile} clientId={clientId} />
        </div>
      </div>
    </div>
  );
}
```
  </action>
  <verify>
    <automated>pnpm tsc --noEmit src/routes/_app/clients/\$clientId/voice/index.tsx</automated>
  </verify>
  <done>Voice settings route exists and renders wizard or tabs based on profile state</done>
</task>

<task type="auto">
  <name>Task 2: Build core components - Summary, Tabs, Wizard</name>
  <files>src/client/components/voice/VoiceProfileSummary.tsx, src/client/components/voice/VoiceSettingsTabs.tsx, src/client/components/voice/VoiceWizard.tsx, src/client/components/voice/index.ts</files>
  <action>
**VoiceProfileSummary.tsx** - Always-visible sidebar card:

```typescript
import { Card, CardContent, CardHeader, CardTitle } from "@/client/components/ui/card";
import { Badge } from "@/client/components/ui/badge";
import { formatDistanceToNow } from "date-fns";
import type { VoiceProfileSelect } from "@/db/voice-schema";

interface Props {
  profile: VoiceProfileSelect;
}

export function VoiceProfileSummary({ profile }: Props) {
  return (
    <Card className="sticky top-4">
      <CardHeader>
        <CardTitle className="text-lg">Voice Profile</CardTitle>
      </CardHeader>
      <CardContent className="space-y-4">
        {/* Mode badge */}
        <div>
          <span className="text-sm text-muted-foreground">Mode</span>
          <Badge variant={getModeVariant(profile.mode)} className="ml-2">
            {profile.mode.replace('_', ' ')}
          </Badge>
        </div>
        
        {/* Confidence */}
        <div>
          <span className="text-sm text-muted-foreground">Confidence</span>
          <div className="flex items-center gap-2">
            <Progress value={profile.confidenceScore} className="h-2" />
            <span className="text-sm font-medium">{profile.confidenceScore}%</span>
          </div>
          {profile.confidenceScore < 70 && (
            <p className="text-xs text-amber-600 mt-1">
              Below 70% - review recommended
            </p>
          )}
        </div>
        
        {/* Quick stats */}
        <div className="grid grid-cols-2 gap-2 text-sm">
          <div>
            <span className="text-muted-foreground">Tone</span>
            <p className="font-medium">{profile.tonePrimary}</p>
          </div>
          <div>
            <span className="text-muted-foreground">Archetype</span>
            <p className="font-medium capitalize">{profile.archetype}</p>
          </div>
          <div>
            <span className="text-muted-foreground">Formality</span>
            <p className="font-medium">{profile.formalityLevel}/10</p>
          </div>
          <div>
            <span className="text-muted-foreground">Contractions</span>
            <p className="font-medium capitalize">{profile.contractionUsage}</p>
          </div>
        </div>
        
        {/* Last analysis */}
        {profile.analyzedAt && (
          <div className="text-xs text-muted-foreground">
            Last analyzed {formatDistanceToNow(profile.analyzedAt, { addSuffix: true })}
          </div>
        )}
      </CardContent>
    </Card>
  );
}
```

**VoiceSettingsTabs.tsx** - Tabbed interface:

```typescript
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/client/components/ui/tabs";
import { ToneTab } from "./ToneTab";
import { VocabularyTab } from "./VocabularyTab";
import { WritingTab } from "./WritingTab";
import { ProtectionTab } from "./ProtectionTab";
import { VoicePreviewSuite } from "./VoicePreviewSuite";

interface Props {
  profile: VoiceProfileSelect;
  clientId: string;
}

export function VoiceSettingsTabs({ profile, clientId }: Props) {
  return (
    <Tabs defaultValue="tone" className="w-full">
      <TabsList className="grid w-full grid-cols-5">
        <TabsTrigger value="tone">Tone</TabsTrigger>
        <TabsTrigger value="vocabulary">Vocabulary</TabsTrigger>
        <TabsTrigger value="writing">Writing</TabsTrigger>
        <TabsTrigger value="protection">Protection</TabsTrigger>
        <TabsTrigger value="preview">Preview</TabsTrigger>
      </TabsList>
      
      <TabsContent value="tone">
        <ToneTab profile={profile} clientId={clientId} />
      </TabsContent>
      
      <TabsContent value="vocabulary">
        <VocabularyTab profile={profile} clientId={clientId} />
      </TabsContent>
      
      <TabsContent value="writing">
        <WritingTab profile={profile} clientId={clientId} />
      </TabsContent>
      
      <TabsContent value="protection">
        <ProtectionTab profile={profile} clientId={clientId} />
      </TabsContent>
      
      <TabsContent value="preview">
        <VoicePreviewSuite profile={profile} clientId={clientId} />
      </TabsContent>
    </Tabs>
  );
}
```

**VoiceWizard.tsx** - Guided mode selection:

```typescript
import { useState } from "react";
import { useMutation, useQueryClient } from "@tanstack/react-query";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/client/components/ui/card";
import { Button } from "@/client/components/ui/button";
import { RadioGroup, RadioGroupItem } from "@/client/components/ui/radio-group";
import { Label } from "@/client/components/ui/label";
import { createVoiceProfileFn, queueVoiceAnalysisFn } from "@/serverFunctions/voice";
import { INDUSTRY_TEMPLATES } from "@/server/features/voice/templates/industryTemplates";

interface Props {
  clientId: string;
}

type Step = 'intro' | 'has_content' | 'preserve_sections' | 'select_mode' | 'select_template' | 'learn_voice';

export function VoiceWizard({ clientId }: Props) {
  const [step, setStep] = useState<Step>('intro');
  const [hasContent, setHasContent] = useState<boolean | null>(null);
  const [preserveSections, setPreserveSections] = useState<boolean | null>(null);
  const [selectedMode, setSelectedMode] = useState<string>('best_practices');
  const [selectedTemplate, setSelectedTemplate] = useState<string | null>(null);
  const queryClient = useQueryClient();
  
  const createMutation = useMutation({
    mutationFn: createVoiceProfileFn,
    onSuccess: () => queryClient.invalidateQueries({ queryKey: ["voice-profile", clientId] }),
  });
  
  // Decision tree logic
  const getRecommendedMode = () => {
    if (!hasContent) return 'best_practices';
    if (preserveSections) return 'preservation';
    return 'application';
  };
  
  const renderStep = () => {
    switch (step) {
      case 'intro':
        return (
          <div className="space-y-4">
            <p>Let's set up your client's brand voice. This will help generate content that matches their style.</p>
            <Button onClick={() => setStep('has_content')}>Get Started</Button>
          </div>
        );
      
      case 'has_content':
        return (
          <div className="space-y-4">
            <h3 className="font-medium">Does this client have existing website content?</h3>
            <RadioGroup onValueChange={(v) => { setHasContent(v === 'yes'); setStep('preserve_sections'); }}>
              <div className="flex items-center space-x-2">
                <RadioGroupItem value="yes" id="yes" />
                <Label htmlFor="yes">Yes, they have established content</Label>
              </div>
              <div className="flex items-center space-x-2">
                <RadioGroupItem value="no" id="no" />
                <Label htmlFor="no">No, starting fresh</Label>
              </div>
            </RadioGroup>
          </div>
        );
      
      // ... more steps for preserve_sections, select_mode, select_template, learn_voice
      
      case 'select_template':
        return (
          <div className="space-y-4">
            <h3 className="font-medium">Select an industry template (optional)</h3>
            <div className="grid grid-cols-2 gap-4">
              {INDUSTRY_TEMPLATES.map(template => (
                <Card 
                  key={template.id}
                  className={`cursor-pointer ${selectedTemplate === template.id ? 'ring-2 ring-primary' : ''}`}
                  onClick={() => setSelectedTemplate(template.id)}
                >
                  <CardHeader className="pb-2">
                    <CardTitle className="text-base">{template.name}</CardTitle>
                    <CardDescription>{template.description}</CardDescription>
                  </CardHeader>
                  <CardContent>
                    <p className="text-sm italic text-muted-foreground">
                      "{template.exampleContent}"
                    </p>
                  </CardContent>
                </Card>
              ))}
            </div>
            <Button onClick={() => handleCreate()}>
              {selectedTemplate ? 'Create from Template' : 'Start from Scratch'}
            </Button>
          </div>
        );
    }
  };
  
  return (
    <Card>
      <CardHeader>
        <CardTitle>Voice Setup Wizard</CardTitle>
        <CardDescription>Configure how content should sound for this client</CardDescription>
      </CardHeader>
      <CardContent>
        {renderStep()}
      </CardContent>
    </Card>
  );
}
```

Create index.ts barrel export.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit src/client/components/voice/VoiceSettingsTabs.tsx src/client/components/voice/VoiceWizard.tsx</automated>
  </verify>
  <done>Core components compile, wizard implements decision tree flow</done>
</task>

<task type="auto">
  <name>Task 3: Build tab content components and preview suite</name>
  <files>src/client/components/voice/ToneTab.tsx, src/client/components/voice/VocabularyTab.tsx, src/client/components/voice/WritingTab.tsx, src/client/components/voice/ProtectionTab.tsx, src/client/components/voice/VoicePreviewSuite.tsx, src/client/components/voice/ProtectionRulesEditor.tsx</files>
  <action>
**ToneTab.tsx:**
- Mode selection radio buttons
- Primary/secondary tone inputs  
- Formality slider (1-10)
- Archetype dropdown
- Personality traits multi-select/tags
- "Learn Voice" button that triggers analysis job with progress bar

**VocabularyTab.tsx:**
- Preferred words tag input (add/remove)
- Avoided words tag input (add/remove)
- Signature phrases textarea (one per line)
- Forbidden phrases textarea (one per line)

**WritingTab.tsx:**
- Sentence length slider with live preview
- Paragraph length slider
- Contraction usage radio (never/sometimes/frequently)
- Heading style dropdown (title_case/sentence_case/all_caps)

**ProtectionTab.tsx:**
- ProtectionRulesEditor component
- Bulk import CSV button
- Rule list with test/delete actions

**ProtectionRulesEditor.tsx:**
```typescript
interface Props {
  profileId: string;
}

export function ProtectionRulesEditor({ profileId }: Props) {
  const { data: rules } = useQuery({
    queryKey: ["protection-rules", profileId],
    queryFn: () => getProtectionRulesFn({ data: { profileId } }),
  });
  
  const createMutation = useMutation({ mutationFn: createProtectionRuleFn });
  const deleteMutation = useMutation({ mutationFn: deleteProtectionRuleFn });
  const importMutation = useMutation({ mutationFn: importProtectionRulesCsvFn });
  
  const [testResult, setTestResult] = useState<{ matched: boolean; preview: string } | null>(null);
  
  return (
    <Card>
      <CardHeader>
        <div className="flex justify-between items-center">
          <CardTitle>Protection Rules</CardTitle>
          <div className="space-x-2">
            <Button variant="outline" onClick={handleCsvImport}>
              Import CSV
            </Button>
            <Button onClick={() => setShowAddDialog(true)}>
              Add Rule
            </Button>
          </div>
        </div>
      </CardHeader>
      <CardContent>
        <Table>
          <TableHeader>
            <TableRow>
              <TableHead>Type</TableHead>
              <TableHead>Target</TableHead>
              <TableHead>Reason</TableHead>
              <TableHead>Expires</TableHead>
              <TableHead>Actions</TableHead>
            </TableRow>
          </TableHeader>
          <TableBody>
            {rules?.map(rule => (
              <TableRow key={rule.id}>
                <TableCell>
                  <Badge>{rule.ruleType}</Badge>
                </TableCell>
                <TableCell className="font-mono text-sm">{rule.target}</TableCell>
                <TableCell>{rule.reason}</TableCell>
                <TableCell>{rule.expiresAt ? format(rule.expiresAt, 'PP') : '-'}</TableCell>
                <TableCell>
                  <Button variant="ghost" size="sm" onClick={() => handleTest(rule)}>
                    Test
                  </Button>
                  <Button variant="ghost" size="sm" className="text-destructive" onClick={() => deleteMutation.mutate(rule.id)}>
                    Delete
                  </Button>
                </TableCell>
              </TableRow>
            ))}
          </TableBody>
        </Table>
        
        {/* Test result preview */}
        {testResult && (
          <Alert className="mt-4">
            <AlertDescription>
              Rule {testResult.matched ? 'matches' : 'does not match'}:
              <pre className="mt-2 bg-muted p-2 rounded">{testResult.preview}</pre>
            </AlertDescription>
          </Alert>
        )}
      </CardContent>
    </Card>
  );
}
```

**VoicePreviewSuite.tsx:**
```typescript
export function VoicePreviewSuite({ profile, clientId }: Props) {
  const [samples, setSamples] = useState<{ headline: string; paragraph: string; cta: string } | null>(null);
  const [compliance, setCompliance] = useState<ComplianceScore | null>(null);
  
  const generateMutation = useMutation({
    mutationFn: generateVoicePreviewFn,
    onSuccess: (data) => {
      setSamples(data.samples);
      setCompliance(data.compliance);
    },
  });
  
  return (
    <Card>
      <CardHeader>
        <CardTitle>Voice Preview</CardTitle>
        <CardDescription>
          Generate sample content to see how your voice settings work
        </CardDescription>
      </CardHeader>
      <CardContent className="space-y-6">
        <Button 
          onClick={() => generateMutation.mutate({ profileId: profile.id })}
          disabled={generateMutation.isPending}
        >
          {generateMutation.isPending ? (
            <>
              <Loader2 className="w-4 h-4 mr-2 animate-spin" />
              Generating...
            </>
          ) : (
            'Generate Samples'
          )}
        </Button>
        
        {samples && (
          <div className="space-y-4">
            {/* Headline sample */}
            <div>
              <Label>Headline</Label>
              <div className="p-3 bg-muted rounded-md mt-1">
                {samples.headline}
              </div>
            </div>
            
            {/* Paragraph sample */}
            <div>
              <Label>Paragraph</Label>
              <div className="p-3 bg-muted rounded-md mt-1">
                {samples.paragraph}
              </div>
            </div>
            
            {/* CTA sample */}
            <div>
              <Label>Call to Action</Label>
              <div className="p-3 bg-muted rounded-md mt-1">
                {samples.cta}
              </div>
            </div>
            
            {/* Compliance scores */}
            {compliance && (
              <div className="border rounded-lg p-4">
                <h4 className="font-medium mb-3">Compliance Scores</h4>
                <div className="grid grid-cols-5 gap-4">
                  {['tone_match', 'vocabulary_match', 'structure_match', 'personality_match', 'rule_compliance'].map(dim => (
                    <div key={dim} className="text-center">
                      <div className="text-2xl font-bold">{compliance[dim]}</div>
                      <div className="text-xs text-muted-foreground">
                        {dim.replace('_', ' ')}
                      </div>
                    </div>
                  ))}
                </div>
                
                {compliance.violations.length > 0 && (
                  <div className="mt-4">
                    <h5 className="text-sm font-medium text-amber-600">
                      {compliance.violations.length} violation(s) found
                    </h5>
                    <ul className="text-sm mt-2 space-y-1">
                      {compliance.violations.map((v, i) => (
                        <li key={i} className="text-muted-foreground">
                          Line {v.line_number}: {v.suggestion}
                        </li>
                      ))}
                    </ul>
                  </div>
                )}
              </div>
            )}
          </div>
        )}
      </CardContent>
    </Card>
  );
}
```

Add generateVoicePreviewFn to serverFunctions/voice.ts:
```typescript
export const generateVoicePreviewFn = createServerFn({ method: "POST" })
  .validator(z.object({ profileId: z.string() }))
  .handler(async ({ data }) => {
    const profile = await voiceProfileService.getById(data.profileId);
    const constraints = buildVoiceConstraints({ profile });
    
    // Generate 3 samples via Claude
    const samples = await generateSamplesWithAI(constraints);
    
    // Score each sample
    const allContent = `${samples.headline}\n\n${samples.paragraph}\n\n${samples.cta}`;
    const compliance = await voiceComplianceService.scoreContent(allContent, profile);
    
    return { samples, compliance };
  });
```
  </action>
  <verify>
    <automated>pnpm tsc --noEmit src/client/components/voice/*.tsx</automated>
  </verify>
  <done>All tab components and preview suite compile and render correctly</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete voice settings UI with:
- Tabbed interface (Tone, Vocabulary, Writing, Protection, Preview)
- Always-visible profile summary sidebar
- Guided wizard for new profiles
- Preview suite with sample generation and compliance scoring
- Protection rules editor with CSV import and test functionality
  </what-built>
  <how-to-verify>
1. Navigate to `/clients/{clientId}/voice` for a client without a voice profile
2. Verify wizard appears with decision tree flow
3. Complete wizard selecting "Application" mode and a template
4. Verify tabbed interface appears with profile summary sidebar
5. Test each tab:
   - Tone: Change formality slider, save
   - Vocabulary: Add preferred/avoided words
   - Writing: Adjust sentence length
   - Protection: Add a page rule, test it
   - Preview: Generate samples, verify compliance scores appear
6. Test "Learn Voice" button shows progress during analysis
7. Verify mobile responsiveness (sidebar collapses appropriately)
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues with the UI</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User -> UI | User inputs voice settings, protection rules |
| UI -> Server | Form data submitted via server functions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-11 | T (Tampering) | Tab forms | mitigate | All updates go through server functions with Zod validation |
| T-37-12 | D (DoS) | generateVoicePreviewFn | mitigate | Rate limit preview generation (max 10/minute/client) |
| T-37-13 | I (Injection) | ProtectionRulesEditor | mitigate | Sanitize CSS selector/regex inputs before display; validate server-side |
</threat_model>

<verification>
- [ ] Voice settings page loads at correct route
- [ ] Wizard appears for clients without profiles
- [ ] All 5 tabs render with correct content
- [ ] Profile summary sidebar updates after changes
- [ ] Preview suite generates samples and shows compliance
- [ ] Protection rules can be added, tested, deleted
- [ ] CSV import works with error reporting
</verification>

<success_criteria>
- Voice settings accessible at /clients/[clientId]/settings/voice (or /clients/[clientId]/voice)
- Tabbed interface with all required tabs
- Preview suite generates headline/paragraph/CTA with compliance scores
- Visual protection rules editor with test functionality
- Guided wizard for mode selection
- Real-time progress during voice learning
- Human verification confirms UI is usable and complete
</success_criteria>

<output>
After completion, create `.planning/phases/37-brand-voice/37-05-SUMMARY.md`
</output>
