# Task 28-03: Keyword Gap Analysis UI Components

## Objective

Create UI components to display keyword gap analysis results with sorting, filtering, and export capabilities. This task implements the frontend components for visualizing competitor keyword gaps and identifying opportunities.

## Tasks

### 1. Create KeywordGapTable component (TEST FIRST)
- [ ] Write component test for KeywordGapTable rendering
- [ ] Create `src/client/components/prospects/KeywordGapTable.tsx`
  - Props: `gaps: KeywordGap[]`, `onAddTarget?: (keyword: string) => void`
  - Columns: Keyword, Competitor, Position, Volume, CPC, Difficulty, Opportunity Score
  - Sortable by all columns (default: Opportunity desc)
  - Difficulty badge component (color-coded by range)
- [ ] Test validates sorting functionality
- [ ] Test validates difficulty badge colors (Easy/Medium/Hard)

### 2. Create DifficultyBadge component (TEST FIRST)
- [ ] Write component test for DifficultyBadge
- [ ] Create `src/client/components/prospects/DifficultyBadge.tsx`
  - Props: `difficulty: number`
  - Color scheme:
    - 0-30: green (Easy)
    - 31-60: yellow (Medium)
    - 61-100: red (Hard)
  - Uses shadcn/ui Badge component
- [ ] Test validates color mapping
- [ ] Test validates edge cases (null, out of range)

### 3. Add gap analysis tab to prospect detail page (TEST FIRST)
- [ ] Write test for tab rendering and data loading
- [ ] Find or create prospect detail page route
- [ ] Add "Keyword Gaps" tab to existing tabs component
- [ ] Fetch gap analysis data from analysis record
- [ ] Display KeywordGapTable with gaps
- [ ] Show summary stats (total gaps, avg opportunity)
- [ ] Test validates data loading and error states

### 4. Add CSV export functionality (TEST FIRST)
- [ ] Write test for CSV export generation
- [ ] Create `exportKeywordGaps(gaps: KeywordGap[]): string` utility
  - Generates CSV with headers and data rows
  - Includes all columns from table
- [ ] Add "Export CSV" button to gap analysis tab
- [ ] Trigger browser download on click
- [ ] Test validates CSV format and content

### 5. Add "Add to targets" action (optional feature)
- [ ] Create placeholder for future target management integration
- [ ] Add button to each row (disabled with tooltip: "Coming soon")
- [ ] Note in code: Integration point for Phase 30+ target management

## Test Strategy

- Component tests using React Testing Library
- Test sorting and filtering logic
- Test CSV export output format
- Visual regression tests for badges (optional)

## Success Criteria

- [ ] KeywordGapTable renders correctly with mock data
- [ ] DifficultyBadge shows correct colors for different ranges
- [ ] Gap analysis tab integrated into prospect detail page
- [ ] CSV export generates valid file
- [ ] All tests pass with 80%+ coverage
- [ ] UI matches shadcn/ui design patterns

## Files Modified

- `src/client/components/prospects/KeywordGapTable.tsx` - NEW: Gap table component
- `src/client/components/prospects/KeywordGapTable.test.tsx` - NEW: Tests
- `src/client/components/prospects/DifficultyBadge.tsx` - NEW: Badge component
- `src/client/components/prospects/DifficultyBadge.test.tsx` - NEW: Tests
- `src/client/utils/export.ts` - NEW: CSV export utility
- `src/client/utils/export.test.ts` - NEW: Tests
- `src/routes/_app/prospects/$prospectId.tsx` - Add gap analysis tab (if exists)
- OR `src/routes/_app/prospects/[prospectId]/index.tsx` - Add gap analysis tab

## Dependencies

- Task 28-02 must be complete (service layer provides data)
- Existing prospect detail page structure
- shadcn/ui components (Badge, Table, Button)
- TanStack Start routing

## Notes

- Use TanStack Table for sorting functionality
- Follow existing component patterns in codebase
- Responsive design for mobile viewing
- Accessibility: proper ARIA labels and keyboard navigation
- Export filename: `keyword-gaps-{domain}-{date}.csv`
