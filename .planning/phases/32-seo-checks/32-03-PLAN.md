---
phase: 32-seo-checks
plan: 03
type: execute
wave: 2
depends_on: [32-01]
files_modified:
  - src/server/lib/audit/checks/tier2/content-quality.ts
  - src/server/lib/audit/checks/tier2/anchor-analysis.ts
  - src/server/lib/audit/checks/tier2/schema-completeness.ts
  - src/server/lib/audit/checks/tier2/freshness.ts
  - src/server/lib/audit/checks/tier2/mobile.ts
  - src/server/lib/audit/checks/tier2/index.ts
autonomous: true
requirements: [CHK-01]

must_haves:
  truths:
    - "All 21 Tier 2 checks execute in <500ms total"
    - "Readability score uses Flesch-Kincaid formula"
    - "Keyword density calculation is accurate"
  artifacts:
    - path: "src/server/lib/audit/checks/tier2/index.ts"
      provides: "Tier 2 check registration"
      exports: ["tier2Checks"]
  key_links:
    - from: "tier2/index.ts"
      to: "registry.ts"
      via: "registers all 21 checks"
---

<objective>
Implement all 21 Tier 2 light calculation checks. These require computation but no external API calls.

Purpose: Tier 2 provides 10 of 40 variable points in the scoring system.
Output: 21 registered checks across 5 categories.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-seo-checks/32-RESEARCH.md
@docs/MICRO-OPTIMIZATIONS-80-PERCENT.md (Tier 2 section)
@.planning/phases/32-seo-checks/32-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Content Quality and Anchor Analysis (8 checks)</name>
  <files>
    src/server/lib/audit/checks/tier2/content-quality.ts,
    src/server/lib/audit/checks/tier2/anchor-analysis.ts
  </files>
  <action>
    **L. Content Quality Metrics (5 checks: T2-01 to T2-05)**
    - T2-01: Reading level <= Grade 9 - Flesch-Kincaid formula:
      `206.835 - 1.015*(words/sentences) - 84.6*(syllables/words)`
      Grade = 0.39*(words/sentences) + 11.8*(syllables/words) - 15.59
      Use syllable counting: count vowel groups, subtract silent e, add 1 for minimum
    - T2-02: Keyword density < 3% - `(keywordCount / totalWords) * 100`
    - T2-03: Word count by query type - compare to benchmark ranges (info: 1500-2500, transactional: 500-1000)
    - T2-04: Statistics every 150-200 words - regex for numbers with context
    - T2-05: Section word count 167-278 - split by H2, count words per section

    **M. Anchor Text Analysis (3 checks: T2-06 to T2-08)**
    - T2-06: >=10 unique anchor variations - count distinct anchor texts
    - T2-07: 50% exact / 25% branded / 25% misc ratio - categorize and calculate
    - T2-08: Links evenly distributed - calculate position percentiles, flag if >50% in one quartile
  </action>
  <verify>
    <automated>npm test -- --run src/server/lib/audit/checks/tier2/content src/server/lib/audit/checks/tier2/anchor</automated>
  </verify>
  <done>8 checks implemented with accurate calculations</done>
</task>

<task type="auto">
  <name>Task 2: Implement Schema, Freshness, and Mobile checks (13 checks)</name>
  <files>
    src/server/lib/audit/checks/tier2/schema-completeness.ts,
    src/server/lib/audit/checks/tier2/freshness.ts,
    src/server/lib/audit/checks/tier2/mobile.ts
  </files>
  <action>
    **N. Schema Completeness (6 checks: T2-09 to T2-14)**
    - T2-09: author.url to author page - validate URL format
    - T2-10: author.sameAs has 3+ links - count array length
    - T2-11: author.sameAs includes LinkedIn - check for linkedin.com
    - T2-12: Organization sameAs array - check for Wikipedia, LinkedIn, Twitter
    - T2-13: publisher.logo >= 112x112px - parse dimensions (may need to fetch or check markup)
    - T2-14: citation array on YMYL - check for citation property in schema

    **O. Freshness Signals (3 checks: T2-15 to T2-17)**
    - T2-15: Visible date matches schema date - compare byline text to dateModified
    - T2-16: sitemap lastmod matches schema - requires sitemap data in context
    - T2-17: No date-only updates - flag if dateModified changed but content hash same (requires historical data)

    **P. Mobile Checks (4 checks: T2-18 to T2-21)**
    - T2-18: H1 above fold on mobile - estimate based on DOM position (<500px from top)
    - T2-19: No interstitials on load - detect overlay/modal patterns in HTML
    - T2-20: Tap targets >= 48px - check button/link sizes in CSS (inline styles or class patterns)
    - T2-21: Text >= 16px on mobile - check font-size in styles

    Note: T2-15, T2-16, T2-17 may return "skipped" if required context unavailable.
    Note: Mobile checks use heuristics from HTML structure; full accuracy requires rendering.
  </action>
  <verify>
    <automated>npm test -- --run src/server/lib/audit/checks/tier2/schema src/server/lib/audit/checks/tier2/freshness src/server/lib/audit/checks/tier2/mobile</automated>
  </verify>
  <done>13 checks implemented with appropriate skip handling</done>
</task>

<task type="auto">
  <name>Task 3: Create Tier 2 index and verify performance</name>
  <files>src/server/lib/audit/checks/tier2/index.ts</files>
  <action>
    1. Create index.ts that imports all 5 category files
    2. Export tier2Checks array with all 21 CheckDefinitions
    3. Auto-register all checks on import
    4. Add performance test: run all 21 checks on sample HTML, assert <500ms
    5. Test edge cases: empty content, missing schema, no keywords
  </action>
  <verify>
    <automated>npm test -- --run src/server/lib/audit/checks/tier2/index.test.ts</automated>
  </verify>
  <done>All 21 Tier 2 checks registered, execute in <500ms</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Schema data | Untrusted JSON-LD from pages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-05 | Tampering | Schema URLs | mitigate | Validate URL format before treating as valid |
</threat_model>

<verification>
- `npm test -- --run src/server/lib/audit/checks/tier2` passes
- Flesch-Kincaid calculation matches reference implementation
- All 21 checks handle missing data gracefully
</verification>

<success_criteria>
- 21 checks implemented across 5 category files
- Each check has id T2-01 through T2-21
- Flesch-Kincaid formula correctly implemented
- Keyword density accurate to 2 decimal places
- Performance <500ms verified by test
</success_criteria>

<output>
After completion, create `.planning/phases/32-seo-checks/32-03-SUMMARY.md`
</output>
