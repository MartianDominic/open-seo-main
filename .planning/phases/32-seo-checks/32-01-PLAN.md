---
phase: 32-seo-checks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/dashboard-schema.ts
  - src/server/lib/audit/checks/types.ts
  - src/server/lib/audit/checks/registry.ts
  - src/server/lib/audit/checks/runner.ts
  - src/server/lib/audit/checks/scoring.ts
  - src/server/lib/audit/checks/scoring.test.ts
autonomous: true
requirements: [CHK-01, CHK-02, CHK-03, CHK-04]

must_haves:
  truths:
    - "audit_findings table stores granular check results per page"
    - "CheckRunner executes registered checks against HTML"
    - "Score calculator applies tier weights and hard gates"
  artifacts:
    - path: "src/db/dashboard-schema.ts"
      provides: "audit_findings table schema"
      contains: "auditFindings"
    - path: "src/server/lib/audit/checks/types.ts"
      provides: "CheckDefinition, CheckResult, CheckContext interfaces"
      exports: ["CheckDefinition", "CheckResult", "CheckContext"]
    - path: "src/server/lib/audit/checks/runner.ts"
      provides: "runChecks function"
      exports: ["runChecks"]
    - path: "src/server/lib/audit/checks/scoring.ts"
      provides: "calculateOnPageScore function"
      exports: ["calculateOnPageScore"]
  key_links:
    - from: "runner.ts"
      to: "registry.ts"
      via: "imports registered checks"
    - from: "scoring.ts"
      to: "types.ts"
      via: "uses CheckResult interface"
---

<objective>
Create the foundation for 107 SEO checks: database schema for storing findings, type definitions, check runner infrastructure, and scoring calculator.

Purpose: All subsequent plans (Tier 1-4 checks) depend on this infrastructure.
Output: audit_findings table, CheckRunner interface, scoring system with hard gates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/32-seo-checks/32-RESEARCH.md
@docs/MICRO-OPTIMIZATIONS-80-PERCENT.md
@src/db/dashboard-schema.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create audit_findings schema and check types</name>
  <files>src/db/dashboard-schema.ts, src/server/lib/audit/checks/types.ts</files>
  <behavior>
    - auditFindings table has: id, auditId, pageId, checkId, tier, category, passed, severity, message, details (jsonb), autoEditable, editRecipe, createdAt
    - Indexes on auditId, pageId, checkId, severity+passed
    - CheckDefinition interface: id, name, tier (1-4), category, severity, autoEditable, editRecipe?, run function
    - CheckContext interface: $ (CheerioAPI), html, url, keyword?, pageAnalysis, siteContext?
    - CheckResult interface: checkId, passed, severity, message, details?, autoEditable, editRecipe?
  </behavior>
  <action>
    1. Add auditFindings table to dashboard-schema.ts with all columns and indexes per RESEARCH.md schema
    2. Create src/server/lib/audit/checks/types.ts with interfaces
    3. Export types for use by all tier check implementations
  </action>
  <verify>
    <automated>npx drizzle-kit generate && grep -q "auditFindings" src/db/dashboard-schema.ts</automated>
  </verify>
  <done>audit_findings migration generated, types exported</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create check registry and runner</name>
  <files>src/server/lib/audit/checks/registry.ts, src/server/lib/audit/checks/runner.ts</files>
  <behavior>
    - Registry stores checks by tier and category
    - registerCheck(check: CheckDefinition) adds to registry
    - getChecksByTier(tier: 1|2|3|4) returns CheckDefinition[]
    - runChecks(html, url, options) runs all registered checks, returns CheckResult[]
    - Shared Cheerio instance passed to all checks (no re-parsing)
  </behavior>
  <action>
    1. Create registry.ts with Map-based storage, registerCheck, getChecksByTier
    2. Create runner.ts that loads HTML once with cheerio.load(), iterates registered checks
    3. Runner accepts options: { tiers?: number[], keyword?: string, pageAnalysis?: PageAnalysis }
    4. Export runTier1Checks, runTier2Checks convenience functions
  </action>
  <verify>
    <automated>npm test -- --run src/server/lib/audit/checks/runner</automated>
  </verify>
  <done>Registry accepts checks, runner executes them with shared Cheerio instance</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create scoring calculator with hard gates</name>
  <files>src/server/lib/audit/checks/scoring.ts, src/server/lib/audit/checks/scoring.test.ts</files>
  <behavior>
    - Test: Base score 60 when all fundamentals present
    - Test: Tier 1 adds +0.3 per passed check, max 20 points
    - Test: Tier 2 adds +0.5 per passed check, max 10 points
    - Test: Tier 3 adds +0.8 per passed check, max 10 points
    - Test: CWV Poor (T3-01/02/03 critical fail) caps at 75
    - Test: noindex (T1-55 fail) caps at 0
    - Test: No author on YMYL caps at 60
    - Test: Duplicate content >60% caps at 50
  </behavior>
  <action>
    1. Write tests first per TDD - all 8 test cases above
    2. Implement calculateOnPageScore(results: CheckResult[]): ScoreResult
    3. ScoreResult: { score: number, gates: string[], breakdown: { base, tier1, tier2, tier3 } }
    4. Apply hard gates BEFORE final score (gates checked first, then remaining points calculated)
  </action>
  <verify>
    <automated>npm test -- --run src/server/lib/audit/checks/scoring.test.ts</automated>
  </verify>
  <done>All 8 scoring tests pass, hard gates correctly cap scores</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTML input | Untrusted HTML from crawled pages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Tampering | check messages | mitigate | Sanitize check.message before storage/display |
| T-32-02 | DoS | HTML parsing | mitigate | Limit HTML size to 5MB before cheerio.load() |
</threat_model>

<verification>
- `npm test -- --run src/server/lib/audit/checks` passes
- `npx drizzle-kit generate` creates audit_findings migration
- Types compile without errors
</verification>

<success_criteria>
- audit_findings table schema complete with all indexes
- CheckDefinition, CheckResult, CheckContext interfaces exported
- Check registry accepts registrations, returns by tier
- Runner executes checks with shared Cheerio instance
- Scoring calculator passes all 8 test cases including hard gates
</success_criteria>

<output>
After completion, create `.planning/phases/32-seo-checks/32-01-SUMMARY.md`
</output>
