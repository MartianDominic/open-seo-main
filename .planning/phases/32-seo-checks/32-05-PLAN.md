---
phase: 32-seo-checks
plan: 05
type: execute
wave: 4
depends_on: [32-01, 32-02, 32-03, 32-04]
files_modified:
  - src/server/features/audit/services/AuditService.ts
  - src/server/lib/audit/checks/index.ts
  - src/routes/clients/[clientId]/seo/audit/[pageId]/index.tsx
  - src/routes/clients/[clientId]/seo/audit/[pageId]/-components/FindingsPanel.tsx
  - src/routes/clients/[clientId]/seo/audit/[pageId]/-components/ScoreCard.tsx
autonomous: false
requirements: [CHK-02, CHK-03, CHK-04]

must_haves:
  truths:
    - "On-page score displays on audit page detail view"
    - "Findings grouped by category with severity indicators"
    - "Score breakdown shows tier contributions and gates"
  artifacts:
    - path: "src/routes/clients/[clientId]/seo/audit/[pageId]/index.tsx"
      provides: "Audit page detail view"
    - path: "src/routes/clients/[clientId]/seo/audit/[pageId]/-components/FindingsPanel.tsx"
      provides: "Findings list component"
  key_links:
    - from: "AuditService.ts"
      to: "runner.ts"
      via: "imports and executes checks"
    - from: "FindingsPanel.tsx"
      to: "audit_findings table"
      via: "fetches findings by pageId"
---

<objective>
Integrate the scoring system with AuditService and create the findings UI at `/clients/[id]/seo/audit/[pageId]`.

Purpose: Users see on-page scores and detailed findings for each audited page.
Output: Working findings page with score display and categorized issues.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-seo-checks/32-RESEARCH.md
@.planning/phases/32-seo-checks/32-01-SUMMARY.md
@.planning/phases/32-seo-checks/32-02-SUMMARY.md
@.planning/phases/32-seo-checks/32-03-SUMMARY.md
@.planning/phases/32-seo-checks/32-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate checks into AuditService</name>
  <files>
    src/server/features/audit/services/AuditService.ts,
    src/server/lib/audit/checks/index.ts
  </files>
  <action>
    1. Create checks/index.ts that exports:
       - runAllChecks(html, url, options): Promise<CheckResult[]>
       - calculateOnPageScore(results): ScoreResult
       - All tier exports for selective running

    2. Update AuditService to:
       - After page HTML fetched, run Tier 1-2 checks synchronously
       - Store CheckResult[] to audit_findings table
       - Calculate and store on_page_score to audit_pages
       - Store score breakdown in on_page_score_breakdown jsonb column

    3. Add migration to add columns to audit_pages:
       - on_page_score: integer (0-100)
       - on_page_score_breakdown: jsonb

    4. Queue Tier 3-4 checks as separate BullMQ job if APIs configured
  </action>
  <verify>
    <automated>npm test -- --run src/server/features/audit/services/AuditService</automated>
  </verify>
  <done>AuditService runs checks, stores findings, calculates scores</done>
</task>

<task type="auto">
  <name>Task 2: Create findings UI components</name>
  <files>
    src/routes/clients/[clientId]/seo/audit/[pageId]/-components/FindingsPanel.tsx,
    src/routes/clients/[clientId]/seo/audit/[pageId]/-components/ScoreCard.tsx
  </files>
  <action>
    **ScoreCard.tsx:**
    - Display on_page_score as large number with color coding:
      - 90+: green (Excellent)
      - 80-89: blue (Good)
      - 70-79: yellow (Average)
      - <70: red (Poor)
    - Show score breakdown: base (60) + tier contributions
    - Display active hard gates with warning icons
    - Show progress bar visualization

    **FindingsPanel.tsx:**
    - Fetch findings from API: GET /api/audit/pages/[pageId]/findings
    - Group by category (A-V from design doc)
    - Show passed/failed count per category
    - Collapsible sections for each category
    - For each finding:
      - Check name + severity badge (critical/high/medium/low)
      - Pass/fail icon
      - Message explaining the issue
      - "Auto-fix available" badge if autoEditable=true
    - Filter controls: severity, passed/failed, tier
  </action>
  <verify>
    <automated>npm test -- --run src/routes/clients/*/seo/audit/*/</automated>
  </verify>
  <done>FindingsPanel and ScoreCard render correctly</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Findings UI integrated with audit page showing score and 107 checks</what-built>
  <how-to-verify>
    1. Run an audit on a test site
    2. Navigate to /clients/[clientId]/seo/audit/[pageId]
    3. Verify:
       - Score displays with correct color coding
       - Breakdown shows base + tier contributions
       - Findings grouped by category
       - Severity badges display correctly
       - Auto-fix badges show on applicable findings
       - Filters work (severity, pass/fail, tier)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API response | Findings data to frontend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-08 | Information Disclosure | findings API | mitigate | Verify user has access to client before returning findings |
| T-32-09 | Tampering | finding messages | mitigate | HTML-escape messages in FindingsPanel |
</threat_model>

<verification>
- Audit creates findings in database
- Score calculation matches expected values
- UI displays all 107 check categories
</verification>

<success_criteria>
- on_page_score stored in audit_pages
- Findings stored in audit_findings with all fields
- ScoreCard shows score + breakdown + gates
- FindingsPanel groups by category, filters work
- Human verification confirms visual correctness
</success_criteria>

<output>
After completion, create `.planning/phases/32-seo-checks/32-05-SUMMARY.md`
</output>
